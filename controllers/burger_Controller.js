var express = require("express");

var router = express.Router();
// rename
var burger = require("../models/burger.js");

// logic for routing, getting routes to front end to orm















module.exports = router;